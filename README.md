# React 特效灵感网站

一个展示创意 React 特效的灵感平台，可以查看、交互、复制代码和查看源代码。

## ✨ 功能特性

- 🎨 **特效展示** - 精美的特效卡片展示
- 🖱️ **交互体验** - 可以直接与特效进行交互
- 📋 **代码复制** - 一键复制特效代码
- 📄 **源代码查看** - 查看完整的组件和样式代码
- 🔍 **搜索筛选** - 支持搜索、标签筛选、分类筛选
- 🚀 **自动发现** - 按照规范创建组件后自动显示，无需手动注册

## 🛠️ 技术栈

- React 18
- JavaScript (ES6+)
- Vite
- React Router
- CSS3 动画

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 `http://localhost:5173` 查看网站。

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 📁 项目结构

```
src/
├── components/          # 通用组件
│   ├── EffectCard.jsx   # 特效卡片组件
│   ├── CodeViewer.jsx   # 代码查看器组件
│   ├── SearchBar.jsx    # 搜索栏组件
│   └── FilterPanel.jsx  # 筛选面板组件
├── effects/             # 特效组件目录
│   ├── _template/       # 组件开发模板
│   │   ├── index.jsx
│   │   ├── Component.jsx
│   │   └── Component.css
│   └── particle-effect/ # 示例组件
│       ├── index.jsx
│       ├── Component.jsx
│       └── Component.css
├── pages/               # 页面组件
│   ├── Home.jsx         # 首页
│   └── EffectDetail.jsx  # 特效详情页
├── hooks/               # 自定义 Hooks
│   └── useEffects.js    # 特效数据管理 Hook
└── utils/               # 工具函数
    └── effectLoader.js  # 组件自动加载器
```

## 🎯 添加新特效

### 方法一：复制模板（推荐）

1. 复制 `src/effects/_template/` 文件夹
2. 重命名为你的组件名称（使用 kebab-case，如 `my-awesome-effect`）
3. 修改 `index.jsx` 中的元数据
4. 在 `Component.jsx` 中实现你的组件
5. 在 `Component.css` 中添加样式（如果需要）
6. **刷新页面，组件会自动显示！**

### 方法二：手动创建

1. 在 `src/effects/` 下创建新文件夹（使用 kebab-case）
2. 创建 `index.jsx` 文件：

```jsx
import Component from './Component'
import componentCode from './Component.jsx?raw'
import componentCss from './Component.css?raw'

export const meta = {
  id: 'my-effect',
  title: '我的特效',
  description: '特效描述',
  tags: ['标签1', '标签2'],
  category: '分类',
  createdAt: new Date().toISOString(),
}

export default Component
export const code = componentCode
export const css = componentCss
```

3. 创建 `Component.jsx` 实现组件
4. 创建 `Component.css` 添加样式（可选）

### 组件规范

每个组件需要：
- ✅ `index.jsx` - 导出组件和元数据
- ✅ `Component.jsx` - 组件实现
- ⚪ `Component.css` - 样式文件（可选）

详细说明请查看 [组件开发指南](./COMPONENT_GUIDE.md)

## 🎨 界面功能

### 首页
- **搜索栏** - 搜索特效标题、描述、标签
- **筛选面板** - 按分类和标签筛选
- **特效网格** - 卡片式展示所有特效

### 详情页
- **特效预览** - 实时预览特效效果
- **代码查看** - 查看组件和样式代码
- **代码复制** - 一键复制代码到剪贴板

## 📚 文档

- [组件开发指南](./COMPONENT_GUIDE.md) - 详细的组件开发说明

## 📝 许可证

MIT
