# GitHub Pages 部署指南

本项目已配置 GitHub Actions 自动部署到 GitHub Pages。

## 📍 部署地址

部署成功后，你的网站将可以通过以下地址访问：

**https://muliminty.github.io/react-idea/**

## 🚀 部署步骤

### 1. 启用 GitHub Pages

1. 进入你的 GitHub 仓库：https://github.com/Muliminty/react-idea
2. 点击 **Settings**（设置）
3. 在左侧菜单中找到 **Pages**
4. 在 **Source**（源）部分，选择 **GitHub Actions**（不是 branch！）

### 2. 推送代码

将代码推送到 `main` 分支：

```bash
git add .
git commit -m "Fix GitHub Pages deployment"
git push origin main
```

### 3. 查看部署状态

1. 进入仓库的 **Actions** 标签页
2. 查看 **Deploy to GitHub Pages** 工作流运行状态
3. 等待部署完成（通常需要 2-5 分钟）

### 4. 访问网站

部署完成后，访问：
- **https://muliminty.github.io/react-idea/**

## 🔧 配置说明

### Base Path

项目已自动配置 base path 为 `/react-idea/`，这是 GitHub Pages 子路径部署所必需的。

### SPA 路由处理

项目已配置 404.html 来处理单页应用的路由问题：
- 所有路由都会自动回退到 index.html
- 刷新页面不会出现 404 错误
- 构建时会自动创建 404.html 并修复资源路径

### 资源路径

- 所有资源文件（JS、CSS）会自动使用正确的 base path
- 构建时会自动处理资源路径
- 404.html 中的资源路径会自动修复为相对路径

### 路由配置

所有路由都会自动适配 base path，包括：
- `/react-idea/splash` - 启动页
- `/react-idea/home` - 主页
- `/react-idea/effect/:id` - 特效详情页

## ❓ 故障排除

### 资源加载失败（JS/CSS 文件 404）

**症状**：控制台显示资源文件加载失败，如：
```
Failed to load resource: net::ERR_ABORTED 404
app.xxx.js:1 Failed to load resource: the server responded with a status of 404
```

**解决方案**：

1. **清除浏览器缓存**
   - Windows: `Ctrl + Shift + R`
   - Mac: `Cmd + Shift + R`
   - 或使用无痕模式访问

2. **检查构建输出**
   - 在 Actions 中查看构建日志
   - 确认 `dist` 目录中有所有资源文件

3. **验证 base path**
   - 检查 `vite.config.js` 中的 base 配置
   - 确认构建时 `GITHUB_REPOSITORY` 环境变量正确设置

4. **检查资源路径**
   - 打开浏览器开发者工具
   - 查看 Network 标签页
   - 检查失败的资源路径是否正确

### 404 错误

如果页面显示 404：

1. **检查 base path**：确保 `vite.config.js` 中的 base 设置为 `/react-idea/`
2. **检查 GitHub Pages 设置**：确保 Source 选择的是 **GitHub Actions**
3. **检查路由**：确保所有链接使用 React Router 的 `Link` 组件
4. **清除浏览器缓存**：按 `Ctrl+Shift+R` (Windows) 或 `Cmd+Shift+R` (Mac) 强制刷新

### 路由刷新后 404

如果直接访问子路由（如 `/react-idea/home`）显示 404：

1. **检查 404.html**：确保构建后生成了 `dist/404.html` 文件
2. **检查 GitHub Pages 设置**：确保已启用自定义 404 页面
3. **等待部署完成**：首次部署可能需要几分钟

### 部署失败

如果 GitHub Actions 部署失败：

1. 查看 **Actions** 标签页中的错误日志
2. 检查 `package.json` 中的构建脚本
3. 确保所有依赖都已正确安装
4. 检查 Node.js 版本是否兼容

### 首次部署较慢

首次部署可能需要较长时间（5-10 分钟），这是正常的。后续部署会更快。

## 🔍 验证部署

部署成功后，验证以下链接：

- 启动页：https://muliminty.github.io/react-idea/splash
- 主页：https://muliminty.github.io/react-idea/home
- 特效详情：https://muliminty.github.io/react-idea/effect/particle-effect

## 📝 注意事项

- ✅ 确保仓库是**公开的**（或使用 GitHub Pro/Team 账户）
- ✅ 首次部署后，GitHub Pages 可能需要几分钟才能生效
- ✅ 如果修改了 base path，需要重新部署
- ✅ 所有路由都会自动适配 base path
- ✅ **清除浏览器缓存**可以解决大部分资源加载问题
- ✅ 使用无痕模式测试可以避免缓存问题

## 🔗 相关链接

- 仓库地址：https://github.com/Muliminty/react-idea
- GitHub Pages 文档：https://docs.github.com/en/pages
- GitHub Actions 文档：https://docs.github.com/en/actions
